# Server Configuration
NODE_ENV=development
PORT=3002
HOST=0.0.0.0

# Database Configuration
# Default (Docker): host=registry-postgres (unique alias), port=5432
# If running the app outside Docker against the compose Postgres, set DB_HOST=localhost and point DATABASE_URL at the host port (DB_PORT_EXTERNAL, default 5435). Leave DB_PORT_INTERNAL at the container port unless you change the Postgres image port.
DB_HOST=registry-postgres
DB_PORT_INTERNAL=5432
DB_NAME=toip_registry_dev
DB_USER=postgres
DB_PASSWORD=postgres
DATABASE_URL="postgresql://postgres:postgres@registry-postgres:5432/toip_registry_dev?schema=public"
# If you change DB_* above, update DATABASE_URL to match

# Docker Compose Host Port
DB_PORT_EXTERNAL=5435

# Optional: override the connection string used inside Docker (useful for remote DBs)
# DATABASE_URL_OVERRIDE="postgresql://user:password@host:5432/database?schema=public"

# Redis Configuration
# For local development (non-Docker): use localhost:6381
# For Docker Compose: use redis:6379 (will be overridden by docker-compose.yml)
REDIS_URL="redis://:redis@redis:6379"
REDIS_PASSWORD=redis
REDIS_PORT=6381

# Registry Configuration
REGISTRY_DID=did:web:localhost:3002
REGISTRY_NAME="ToIP Trust Registry Dev"

# API Configuration
API_VERSION=v2
API_BASE_PATH=/v2

# Security
API_KEY_SALT_ROUNDS=12
JWT_SECRET=your-secret-key-here-minimum-32-characters-long-for-security

# Logging
LOG_LEVEL=info

# CORS
CORS_ORIGIN=http://localhost:5173

# Rate Limiting
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=100

# Optional: Registry Keys (will be auto-generated if not provided)
# REGISTRY_PRIVATE_KEY=
# REGISTRY_PUBLIC_KEY=

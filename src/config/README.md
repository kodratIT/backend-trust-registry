# Config

This directory contains configuration modules.

## Structure

Configuration modules manage application settings. They should:
- Load environment variables
- Validate configuration
- Provide typed config access
- Handle different environments

## Files

- `env.ts` - Environment variable validation and access

{
  "$id": "trqp-recognition-request",
  "title": "RecognitionRequest",
  "type": "object",
  "required": ["entity_id", "authority_id", "action", "resource"],
  "properties": {
    "entity_id": {
      "type": "string",
      "minLength": 1,
      "description": "The identifier for the entity being tested for recognition"
    },
    "authority_id": {
      "type": "string",
      "minLength": 1,
      "description": "The identifier for the authority being queried"
    },
    "action": {
      "type": "string",
      "minLength": 1,
      "description": "The action to check recognition for"
    },
    "resource": {
      "type": "string",
      "minLength": 1,
      "description": "The resource to check recognition for"
    },
    "context": {
      "type": "object",
      "properties": {
        "time": {
          "type": "string",
          "format": "date-time",
          "description": "Time for query. RFC3339 format"
        }
      },
      "additionalProperties": true
    }
  },
  "additionalProperties": false
}
